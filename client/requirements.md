## Packages
framer-motion | Page transitions and scroll-triggered animations for a premium feel
lucide-react | High-quality icons for services and features
react-hook-form | Form state management
@hookform/resolvers | Zod validation for forms

## Notes
The application layout is configured for Right-to-Left (RTL) to properly support the Arabic content.
The `dir="rtl"` attribute is set in the main App component.
Using 'Cairo' Google Font for beautiful Arabic typography.
